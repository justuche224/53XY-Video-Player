// Thin re-export of the local SystemVolume Expo module (Android system media
// volume via AudioManager). Requires a native build to be present.
export { default as SystemVolume } from '../../modules/system-volume/src/SystemVolumeModule';
