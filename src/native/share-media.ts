// Thin re-export of the local ShareMedia Expo module (Android multi-file share
// via ACTION_SEND_MULTIPLE). Requires a native build to be present.
export { default as ShareMedia } from '../../modules/share-media/src/ShareMediaModule';
