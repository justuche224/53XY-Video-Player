diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/PlayerEvent.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/PlayerEvent.kt
index 5fcf3e5..c1831f9 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/PlayerEvent.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/PlayerEvent.kt
@@ -17,6 +17,7 @@ import expo.modules.video.records.PlaybackError
 import expo.modules.video.records.PlaybackRateChangedEventPayload
 import expo.modules.video.records.SourceChangedEventPayload
 import expo.modules.video.records.StatusChangedEventPayload
+import expo.modules.video.records.SubtitleCueChangedEventPayload
 import expo.modules.video.records.SubtitleTrack
 import expo.modules.video.records.SubtitleTrackChangedEventPayload
 import expo.modules.video.records.AudioTrackChangedEventPayload
@@ -79,6 +80,11 @@ sealed class PlayerEvent {
     override val jsEventPayload = SubtitleTrackChangedEventPayload(subtitleTrack, oldSubtitleTrack)
   }
 
+  data class SubtitleCueChanged(val text: String) : PlayerEvent() {
+    override val name = "subtitleCueChange"
+    override val jsEventPayload = SubtitleCueChangedEventPayload(text)
+  }
+
   data class AudioTrackChanged(val audioTrack: AudioTrack?, val oldAudioTrack: AudioTrack?) : PlayerEvent() {
     override val name = "audioTrackChange"
     override val jsEventPayload = AudioTrackChangedEventPayload(audioTrack, oldAudioTrack)
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt
index 8bae522..beef216 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayer.kt
@@ -15,6 +15,7 @@ import androidx.media3.common.Player.STATE_BUFFERING
 import androidx.media3.common.Timeline
 import androidx.media3.common.TrackSelectionParameters
 import androidx.media3.common.Tracks
+import androidx.media3.common.text.CueGroup
 import androidx.media3.common.util.UnstableApi
 import androidx.media3.exoplayer.DecoderReuseEvaluation
 import androidx.media3.exoplayer.DefaultRenderersFactory
@@ -237,6 +238,18 @@ class VideoPlayer(val context: Context, appContext: AppContext, source: VideoSou
       this@VideoPlayer.playing = isPlaying
     }
 
+    override fun onCues(cueGroup: CueGroup) {
+      // ExoPlayer already decodes and renders the selected embedded subtitle
+      // track; this is the only place its text is observable. Forwarded so JS
+      // can seed a captured moment's note with the line on screen — expo-video
+      // otherwise exposes track selection but never the cues themselves.
+      val text = cueGroup.cues
+        .mapNotNull { it.text?.toString() }
+        .filter { it.isNotBlank() }
+        .joinToString("\n")
+      sendEvent(PlayerEvent.SubtitleCueChanged(text))
+    }
+
     override fun onTracksChanged(tracks: Tracks) {
       val oldSubtitleTracks = ArrayList(subtitles.availableSubtitleTracks)
       val oldAudioTracks = ArrayList(audioTracks.availableAudioTracks)
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/records/VideoEventPayloads.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/records/VideoEventPayloads.kt
index 2cbc397..81def6e 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/records/VideoEventPayloads.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/records/VideoEventPayloads.kt
@@ -50,6 +50,10 @@ class SubtitleTrackChangedEventPayload(
   @Field val oldSubtitleTrack: SubtitleTrack?
 ) : VideoEventPayload
 
+class SubtitleCueChangedEventPayload(
+  @Field val text: String
+) : VideoEventPayload
+
 class AudioTrackChangedEventPayload(
   @Field val audioTrack: AudioTrack?,
   @Field val oldAudioTrack: AudioTrack?
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt
index 460fb7b..ac26191 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/utils/DataSourceUtils.kt
@@ -2,10 +2,13 @@ package expo.modules.video
 
 import android.content.Context
 import android.content.pm.ApplicationInfo
+import android.net.Uri
 import androidx.annotation.OptIn
 import androidx.media3.common.util.UnstableApi
-import androidx.media3.common.util.Util
 import androidx.media3.datasource.DataSource
+import androidx.media3.datasource.DataSpec
+import androidx.media3.datasource.TransferListener
+import androidx.media3.common.util.Util
 import androidx.media3.datasource.DefaultDataSource
 import androidx.media3.datasource.cache.CacheDataSource
 import androidx.media3.datasource.okhttp.OkHttpDataSource
@@ -15,6 +18,68 @@ import expo.modules.video.records.VideoSource
 import expo.modules.video.managers.VideoManager
 import okhttp3.OkHttpClient
 
+// 53XY fix (docs/2026-08-26-playback-startup-latency.md): some 10-bit HEVC .mkv files took
+// 9-12s to start playback. On-device I/O tracing found the cause: ExoPlayer's MatroskaExtractor
+// reads the file's Cues element (near EOF in these remuxes) via ~1-4 byte DataSource.read()
+// calls -- one file took 558,146 individual reads to transfer 781KB, ~12.8s wall time purely
+// from per-call (syscall/JNI) overhead multiplied hundreds of thousands of times. This wraps
+// the real DataSource with a read-ahead buffer so those tiny sequential reads are served from
+// an already-fetched chunk instead of a fresh read each time -- MatroskaExtractor's own Cues
+// parsing and seeking behavior are completely unchanged (unlike disabling seek-for-cues, which
+// was tried first and broke scrubbing by discarding the seek index entirely).
+private const val IO_BUFFER_SIZE = 64 * 1024
+
+@OptIn(UnstableApi::class)
+private class BufferedDataSource(private val delegate: DataSource) : DataSource {
+  private val buffer = ByteArray(IO_BUFFER_SIZE)
+  private var bufferPos = 0
+  private var bufferLen = 0
+
+  override fun addTransferListener(transferListener: TransferListener) =
+    delegate.addTransferListener(transferListener)
+
+  override fun open(dataSpec: DataSpec): Long {
+    bufferPos = 0
+    bufferLen = 0
+    return delegate.open(dataSpec)
+  }
+
+  override fun read(destination: ByteArray, offset: Int, length: Int): Int {
+    if (length == 0) return 0
+    if (bufferPos < bufferLen) {
+      val toCopy = minOf(bufferLen - bufferPos, length)
+      System.arraycopy(buffer, bufferPos, destination, offset, toCopy)
+      bufferPos += toCopy
+      return toCopy
+    }
+    // A request already as large as our buffer gains nothing from buffering -- forward it
+    // directly to avoid an extra copy on the normal (large-read) playback path.
+    if (length >= IO_BUFFER_SIZE) {
+      return delegate.read(destination, offset, length)
+    }
+    val n = delegate.read(buffer, 0, IO_BUFFER_SIZE)
+    if (n <= 0) return n
+    bufferLen = n
+    val toCopy = minOf(n, length)
+    System.arraycopy(buffer, 0, destination, offset, toCopy)
+    bufferPos = toCopy
+    return toCopy
+  }
+
+  override fun getUri(): Uri? = delegate.getUri()
+
+  override fun close() {
+    bufferPos = 0
+    bufferLen = 0
+    delegate.close()
+  }
+}
+
+@OptIn(UnstableApi::class)
+private class BufferedDataSourceFactory(private val delegate: DataSource.Factory) : DataSource.Factory {
+  override fun createDataSource(): DataSource = BufferedDataSource(delegate.createDataSource())
+}
+
 @OptIn(UnstableApi::class)
 fun buildBaseDataSourceFactory(context: Context, videoSource: VideoSource): DataSource.Factory {
   return if (videoSource.uri?.scheme?.startsWith("http") == true) {
@@ -54,7 +119,7 @@ fun buildCacheDataSourceFactory(context: Context, videoSource: VideoSource): Dat
 }
 
 fun buildMediaSourceFactory(context: Context, dataSourceFactory: DataSource.Factory): MediaSource.Factory {
-  return DefaultMediaSourceFactory(context).setDataSourceFactory(dataSourceFactory)
+  return DefaultMediaSourceFactory(context).setDataSourceFactory(BufferedDataSourceFactory(dataSourceFactory))
 }
 
 @OptIn(UnstableApi::class)
diff --git a/node_modules/expo-video/build/VideoPlayerEvents.types.d.ts b/node_modules/expo-video/build/VideoPlayerEvents.types.d.ts
index 2de2f7b..48380ad 100644
--- a/node_modules/expo-video/build/VideoPlayerEvents.types.d.ts
+++ b/node_modules/expo-video/build/VideoPlayerEvents.types.d.ts
@@ -43,6 +43,13 @@ export type VideoPlayerEvents = {
      * Handler for an event emitted when the current subtitle track changes.
      */
     subtitleTrackChange(payload: SubtitleTrackChangeEventPayload): void;
+    /**
+     * Fires when the displayed embedded subtitle cue changes. `text` is '' when
+     * no cue is on screen. Patched in locally; not upstream expo-video.
+     */
+    subtitleCueChange(payload: {
+        text: string;
+    }): void;
     /**
      * Handler for an event emitted when the available audio tracks change.
      */
